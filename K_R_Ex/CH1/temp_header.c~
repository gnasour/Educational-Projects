#include <stdio.h>
#include <stdlib.h>
#define default_rows 10

void calculateTemp(int);

int main(int argc, char**argv){
  if(argc > 1)
    calculateTemp(atoi(argv[2]));
  else
    calculateTemp(default_rows);
  return 0;
}

void calculateTemp(int print_rows){

  for(int i = 0; i < print_rows; i++){
    
  }

}
