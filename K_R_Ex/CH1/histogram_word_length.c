#include <stdio.h>

#define MAXLENGTH 1000 //Max length of 1000 characters and 1000 word lengths

int word_length(char* word_lengths, int word_length_index_size){

}

void print_histogram(char* word_lengths, int word_length_index_size){

}

int main(){
    char word[MAXLENGTH];
    char word_lengths[MAXLENGTH];
    char letter;
    int word_index_size = 0;
    int word_length_index_size = 0;
    while((letter = getchar())!= EOF){

        if(letter == '\n'){

        }
    }
    return 0;
}